{
  "name": "csexpert-heroku",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npx vite build",
    "heroku-postbuild": "cd frontend && npm install --include=dev && npx vite build",
    "start": "echo 'This is handled by Python buildpack'"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "dependencies": {},
  "devDependencies": {}
} 