{
  "name": "csexpert-heroku",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && VITE_API_BASE_URL='' npm install && npm run build",
    "heroku-postbuild": "npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
} 